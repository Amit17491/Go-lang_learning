package main /* In go every program is a part of package. we define this using package keyword */

import "fmt" /* lets us import files included in the fmt packages */

func main() { /* Any code inside this will be executed*/
	fmt.Print("hello World !!") /* so println is the package import from the fmt */
}
